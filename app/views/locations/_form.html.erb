<%= simple_form_for(@location) do |f| %>
  <%= f.error_notification %>
  <%- #debug(@location) if Rails.env.development? -%> 
  <div class="form-inputs">
    <%= f.input :city %>
    <%= f.input :nation %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
