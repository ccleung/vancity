<p>
  <strong>Title:</strong>
  <%= @ad.title %>
</p>
 
<p>
  <strong>Description:</strong>
  <%= @ad.description %>
</p>

<p>
  <strong>UserId:</strong>
  <%= @ad.user_id %>
</p>

<p>
	<strong>Image:</strong>
	<br/>
	<%= image_tag @ad.image.image.url if @ad.image %>
	<br/>
	<%= @ad.image.image.url if @ad.image %>
	<br/>
	<%= @ad.image.image.current_path if @ad.image %>
	<br/>
	<%= @ad.image.image.identifier if @ad.image %>
</p>